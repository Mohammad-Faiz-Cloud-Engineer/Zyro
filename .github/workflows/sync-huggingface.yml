name: Sync to Hugging Face Spaces

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git remote add hf https://LocalFind:${HF_TOKEN}@huggingface.co/spaces/LocalFind/Zyro.git
          git push hf HEAD:main --force
